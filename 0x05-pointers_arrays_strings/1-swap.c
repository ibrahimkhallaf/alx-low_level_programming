#include <main.h>
#include <stdio.h>
/**
 *swap_int -  swaps the values of two integers using to prameters
 *
 * @a: input prameter 1
 * @b: input prameter 2
 *
 * RETURN: Nothing
 */
void swap_int(int *a, int *b)
{
	int temp;

	*a = temp;
	*a = *b;
	*b = temp;
}
